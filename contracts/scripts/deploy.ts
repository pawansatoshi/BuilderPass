/**
 * Deployment script — to be implemented in Milestone 3, once BuilderPassport.sol
 * exists (Milestone 2). Left as a stub so the workspace compiles/runs cleanly
 * during Milestone 1 scaffolding.
 */
async function main() {
  console.log(
    "No contract to deploy yet — BuilderPassport.sol is scheduled for Milestone 2."
  );
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
