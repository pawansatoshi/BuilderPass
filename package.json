{
  "name": "giwa-builder-passport",
  "private": true,
  "version": "0.1.0",
  "description": "GIWA Builder Passport — on-chain builder identity for the GIWA GASOK Builder Program",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "app",
    "contracts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=app",
    "build": "npm run build --workspace=app",
    "contracts:compile": "npm run compile --workspace=contracts",
    "contracts:test": "npm run test --workspace=contracts",
    "contracts:deploy:giwaSepolia": "npm run deploy:giwaSepolia --workspace=contracts"
  }
}
